# Aeshnidae — the website

The player-facing site for Aeshnidae, a private Asheron's Call server. One
static page, served by GitHub Pages from this repo's `main` branch.

Edit `index.html`, commit, push — the site updates within a minute.
